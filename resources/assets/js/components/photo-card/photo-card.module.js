import angular from 'angular'

import { PhotoCardComponent } from './photo-card.component'

export const PhotoCardModule = angular.module('components.photo-card', [
    
])
.component('photoCardComponent', PhotoCardComponent)
.name